spring:
  application:
    name: planner-todo
  rabbitmq:
    host: localhost
    port: 15672
    username: rmuser
    password: rmpassword
  datasource:
    url: jdbc:postgresql://localhost:5432/planner_tasks
    username: root
    password: root


eureka:
  client:
    service-url:
      defautl-zone: http://localhost:8761/eureka


management:
  endpoints:
    web:
      exposure:
        include: "*"

check: some string